<%= form_for [@article, @comment] do |form| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="new_article_label">Your name</div>
  <div class="new_input"><%= form.text_field :user, :maxlength => 20 %>
  </div>

  <div class="new_article_label">Your comment</div>
  <div class="new_comment"><%= form.text_area :body, :maxlength => 200 %></div>

  <div class="new_article_label"><%#= form.label :article_id %></div>
  <div class="new_input"><%#= form.text_field :article_id %></div>

  <div class="new_submit"><%= form.submit %></div>
<% end %>

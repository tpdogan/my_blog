<%= form_with(model: article, local: true) do |form| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="new_article_label"><%= form.label :title %></div>
  <div class="new_input"><%= form.text_field :title %></div>

  <div class="new_article_label"><%= form.label :body %></div>
  <div class="new_body"><%= form.text_area :body %></div>

  <%= notice %>
  <div class="new_article_label">Created by</div>
  <div class="new_input"><%= form.text_field :signature %></div>

  <div class="new_submit"><%= form.submit %></div>
<% end %>
